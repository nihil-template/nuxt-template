# 프로젝트 운영 지침

## 기본 원칙

- 사용자를 마스터로 호칭하고, 진중하고 차분한 전문적 어조의 한글로 응답한다.
- 작업을 시작하기 전에 이 파일과 작업 범위에 관련된 `docs/`, `references/`, 기존 구현을 읽는다.
- 적용 가능한 Skill을 먼저 확인하고 사용한다. 특히 비경량 작업에는 Superpowers 절차를 기본으로 적용한다.
- 명시된 요구사항과 이 문서의 규칙이 충돌하면 마스터의 최신 지시를 우선한다.

## 바이브코딩 기획 구체화 절차

마스터가 서비스·앱의 기초 기획을 제공하고 구체화를 요청하면, 이를 곧바로 구현 명세로 간주하지 않는다. 문답을 통해 제품 결정을 확정하고, 각 설계 단계가 앞 단계의 근거를 추적할 수 있게 기록한 뒤 구현 범위를 정한다.

### 기초 기획의 1차 기획화

- 마스터가 지정한 참고 폴더·문서와 기존 제품 기록을 질문 전에 먼저 확인한다.
- 한 번에 하나의 결정 단위만 묻고, 선택지가 도움이 되면 이해하기 쉬운 선택지를 제시한다.
- 이미 확정된 결정에서 직접 도출되는 내용은 재질문하지 않고 파생 규칙으로 기록한다.
- 마스터의 선택이나 정정은 즉시 `docs/project-design/`의 제품 설계 문서에 반영한다.
- 확정 결정과 미결 결정을 분리하고, 미확정 내용을 추측으로 채우지 않는다.
- 진행 순서·작성 기준·보류 항목은 `docs/work-design/`의 작업 설계 문서에 기록한다.
- 문서 기록 뒤에는 `git diff --check`를 실행해 형식을 확인한다.
- 기획 확정은 화면·API·데이터베이스 또는 코드의 구현 승인을 뜻하지 않는다.

### 기획 보강 단계와 순서

기초 기획은 다음 순서로 보강한다.

1. **1차 기획화:** 기초 기획을 문답으로 구체화하고 확정 결정과 미결 결정을 기록한다.
2. **2차 보강:** 확정 결정을 근거로 사용자 스토리를 작성하고, 사용자 스토리에서 유스케이스를 도출한 뒤 유스케이스를 근거로 데이터 플로우를 작성한다.
3. **3차 보강:** 데이터 플로우를 근거로 데이터 스키마의 엔티티·테이블·컬럼·관계·제약을 구상한다.
4. **4차 기획:** 화면과 API를 설계하기 전에 도메인 책임, 권한 매트릭스, 검증 규칙, 실패 조건, 상태 정의 및 전이를 순서대로 확정한다.
5. **화면·라우트·API 설계:** 확정된 제품 경계를 근거로 화면 역할과 이동 구조, API 계약을 설계하고 유스케이스 추적성을 확인한다.

- 2차 보강의 내부 순서는 반드시 `사용자 스토리 → 유스케이스 → 데이터 플로우`로 한다.
- 각 단계는 바로 앞 단계에서 확정된 내용만을 근거로 삼고, 후행 단계의 내용을 미리 확정하지 않는다.
- 스키마 초안을 작성한 뒤 3차 보강을 확정하기 전에 참조·범위·삭제 처리·제약을 점검한다. 이 점검을 3차 보강 이후의 별도 단계로 미루지 않는다.
- 후행 단계에서 상위 결정의 누락이나 상위 근거가 없는 항목을 발견하면 전체 추적 관계를 확인한다. 확정 정책의 직접적인 누락은 정정하고, 새 제품 판단이 필요할 때만 마스터에게 묻는다.

### 단계별 작성 원칙

#### 1차 기획 결정

- 서비스 전체에 지속되는 제품 결정은 `docs/project-design/`에 기록한다.
- 각 결정은 도메인별 제목 아래 번호를 붙인 짧고 단정적인 문장으로 작성한다.
- 마스터가 직접 선택하거나 명시한 내용과 그 직접적인 파생 규칙을 구분한다.
- 기존 결정이 정정되면 모순되는 과거 문장을 남기지 않고 현재 의도가 드러나게 수정한다.
- 구현 방식, 화면 배치, API, 데이터베이스 구조는 1차 기획 결정에 섞지 않는다.

#### 사용자 스토리

- 기초 기획과 확정된 1차 기획만을 근거로 기능을 추출한다.
- `US-번호. <사용자 역할>로서 <원하는 행동>하고 싶다. <얻는 목적 또는 결과>.` 형식으로 작성한다.
- 한 스토리에는 하나의 사용자 목적이나 결과만 넣고, 독립적인 행동은 분리한다.
- 화면·API·데이터베이스·내부 코드 같은 구현 방법을 본문에 넣지 않는다.
- 근거가 없는 기능은 확정 스토리로 추가하지 않고 별도의 검토 후보로 분리한다.
- 우선순위는 기능 필요성을 나타내며 구현 승인이나 실제 개발 일정을 뜻하지 않는다.

#### 유스케이스

- 하나의 유스케이스는 하나의 명확한 사용자 목적과 행위자를 다룬다.
- 각 유스케이스에 대응하는 `US-번호`를 기록한다.
- 필요한 범위에서 이름, 행위자, 목적, 사전 조건, 기본 흐름, 대안 흐름, 예외 흐름, 결과, 관련 데이터를 작성한다.
- 사용자의 행동과 시스템 반응을 서술하되 화면 배치, 컴포넌트, HTTP 메서드, ORM, 프레임워크 같은 구현 세부 사항은 넣지 않는다.
- 데이터 구조를 미리 확정하지 않고 필요한 데이터의 의미와 상태 변화만 식별한다.
- 새 요구사항이나 정책 판단이 필요하면 임의로 결정하지 않고 미결 사항으로 분리한다.
- 작성이 끝나면 모든 `UC-번호`가 하나 이상의 확정 `US-번호`와 연결되는지 확인한다.

#### 데이터 플로우

- 유스케이스가 확정된 뒤 각 흐름이 어떤 데이터를 읽고, 검증하고, 만들고, 바꾸고, 보존하는지 작성한다.
- 유스케이스 하나 또는 강하게 연결된 묶음마다 대응하는 `UC-번호`와 `US-번호`를 기록한다.
- 각 플로우는 `주체와 시작 조건 → 읽기·검증 → 생성·변경·보존 데이터 → 결과와 후속 상태` 순으로 작성한다.
- 현재 상태와 리비전·공개본·감사 이력처럼 보존할 과거 상태를 구분한다.
- 권한, 소유 범위, 공개 여부, 삭제 상태처럼 처리 결과를 바꾸는 조건을 명시한다.
- 데이터 플로우에는 테이블, 컬럼, ORM, API 같은 구현 구조를 넣지 않는다.

#### 데이터 스키마

- 데이터 플로우가 확정되면 데이터의 식별·범위·상태·이력·연결을 엔티티와 테이블·컬럼 수준으로 구상한다.
- 주요 엔티티·상태·관계·제약이 어떤 데이터 플로우에서 요구됐는지 추적할 수 있게 한다.
- 스키마 초안 뒤에는 테이블 간 참조, 소유 범위, 삭제·복구 영향, 중복, 상태 제약을 점검한다.
- 점검에서 새로운 제품 정책이 필요하면 구조로 우회하지 않고 마스터와 먼저 결정한다.
- 스키마 구상은 ORM 코드, 마이그레이션, DDL 또는 실제 데이터베이스 변경 승인을 뜻하지 않는다.

#### 구현 경계 설계

3차 보강과 추적성 점검을 마친 뒤 다음 순서로 제품 경계를 확정한다.

1. **도메인 책임 정의:** 각 도메인이 소유하는 개념·상태·행동과 다른 도메인과의 경계를 정한다.
2. **권한 매트릭스:** 역할과 범위별 조회·생성·수정·삭제·특수 행동의 허용 여부를 정한다.
3. **검증 규칙:** 입력값, 범위, 상태 전이, 중복, 참조 정합성의 성공 조건을 정한다.
4. **실패 조건:** 권한 부족, 입력 오류, 상태 충돌, 삭제·비공개·비활성 대상 접근의 시스템 결과를 정한다.
5. **상태 정의 및 전이:** 상태값, 최초 상태, 허용 전이, 전이 주체, 전이 결과와 금지 조건을 정한다.

- 한 상태를 둘 이상의 도메인이 직접 변경하는 것으로 설계하지 않는다. 도메인 간 영향은 상태 전이 또는 참조 규칙으로 명확히 한다.
- 검증 성공 조건과 실패 결과를 섞지 않고 각각의 단계에서 분리해 기록한다.
- 이 다섯 단계가 확정되기 전에는 화면·라우트·API 계약을 확정하지 않는다.
- 화면·라우트·API 설계에서는 각 유스케이스를 하나 이상의 화면과 API 영역에 연결하고, 상위 근거 없는 기능이나 하위 누락을 점검한다.
- 설계 문서의 확정과 실제 파일·핸들러·타입·테스트·ORM·마이그레이션·데이터베이스 변경의 구현 승인을 구분한다.

### 단계 간 추적성

다음 관계를 따라 제품 결정부터 구현 설계까지 근거를 추적한다.

`1차 기획 확정 결정 → US-번호 → UC-번호 → 데이터 플로우 → 데이터 스키마 → 화면·라우트·API 설계`

- 하위 단계의 모든 주요 기능·흐름·데이터 구조는 상위 근거를 가져야 한다.
- 상위 단계에서 확정된 내용이 하위 단계에 빠졌는지 각 단계 종료 시 확인한다.
- 상위 근거가 없는 항목은 임의 추가 여부를 점검하고, 필요한 제품 판단을 마스터와 확정하기 전까지 구현하지 않는다.
- 새 결정이 생기면 영향을 받는 후행 문서와 추적 관계를 함께 갱신한다.

## 작업 분류와 진행 절차

- 실제 수정·생성·삭제 대상 파일이 하나면 경량 작업이다.
- 대상 파일이 둘 이상이면 비경량 작업이다.
- 경량 작업은 현재 세션에서 직접 처리한다. 별도의 실행 방식 선택을 요청하지 않는다.
- 경량 작업도 변경 범위에 맞는 최소 검증을 실행한 후 결과를 보고한다.
- 비경량 작업은 다음 순서를 따른다.
  1. 만들 앱이나 기능의 목적, 사용자, 해결할 문제, 완료 기준을 마스터에게 확인한다.
  2. `superpowers:brainstorming`으로 콘셉트와 범위를 정리하고 설계 승인을 받는다.
  3. 설계 문서를 `docs/superpowers/specs/`에 작성한다.
  4. `superpowers:writing-plans`으로 구현 계획을 `docs/superpowers/plans/`에 작성한다.
  5. 승인된 계획에 따라 구현한다.
  6. 테스트·린트·타입 검사·빌드 중 영향 범위에 적합한 검증을 실행하고, 결과를 근거와 함께 보고한다.
- 이 저장소는 앱 템플릿이다. 새 앱 또는 기능 개발을 시작할 때에는 화면이나 기능을 곧바로 만들지 않고, 먼저 어떤 앱을 만들지 마스터의 의견을 물어 브레인스토밍으로 콘셉트를 구체화한다.

## 문서 관리

- 서비스 전체에 지속되는 제품 원칙, 도메인 구조, 권한, 상태, 데이터 보존 규칙과 확정 요구사항은 `docs/project-design/` 아래 Markdown으로 작성한다.
- 특정 기능이나 변경 작업의 범위, 영향, 진행 순서, 검증 및 완료 기준은 `docs/work-design/` 아래 Markdown으로 작성한다.
- 설계와 구현 계획은 `docs/superpowers/` 아래 Markdown으로 작성한다.
- 제품 설계·작업 설계 문서와 Superpowers 설계·구현 계획 문서는 서로의 역할을 대체하지 않는다.
- `references/`는 개발 중 참고할 자료를 위한 폴더다. 마스터가 제공한 문서, 요구사항, 조사 자료, 외부 레퍼런스를 Markdown으로 보관한다.
- 에이전트가 작성한 분석이나 정리도 마스터가 향후 참고할 가치가 있다고 판단할 때에만 `references/`에 보관한다.
- 지속적으로 적용되는 작업 규칙은 `AGENTS.md`에 기록하고, 특정 기능의 상세 근거는 설계 문서 또는 참고 문서에 둔다.

## 프로젝트 구조

- 루트 구조는 `app/`, `server/`, `public/`을 기본으로 한다.
- `app/` 내부에는 `assets/`, `components/`, `composables/`, `config/`, `data/`, `layouts/`, `pages/`, `types/`, `utils/`를 사용한다.
- Pinia 상태가 필요한 경우에만 `app/stores/`를 추가하며, 페이지 지역 상태나 composable 공유 상태로 해결할 수 있는 경우에는 store를 만들지 않는다.
- `data/`는 앱에서 사용하는 정적 데이터와 데이터 정의를 둔다. 파일명은 반드시 `*.data.ts`로 통일한다.
- 명명된 TypeScript 타입과 인터페이스는 반드시 `app/types/`에 둔다. 구현 파일 내부에 선언하지 않는다.
- `config/`는 동작 설정과 환경별 구성값을 둔다. 비밀값은 클라이언트 코드나 `public` runtime config에 넣지 않는다.
- `app/types/common.types.ts`, `app/composables/useSetMeta.ts`, `app/config/site.config.ts`는 사이트 기본 설정 파일이다. 새 프로젝트 또는 사이트의 목적과 환경에 맞게 반드시 검토·수정하여 사용한다.
- 모든 페이지는 `useSetMeta`를 반드시 호출해 페이지별 제목과 URL을 설정한다. 홈 페이지는 `app/pages/index.vue`의 다음 형태를 기준으로 한다.

```ts
useSetMeta({
  title: '페이지 제목',
  url: '/',
});
```

- 디자인 작업 전에는 `app/assets/styles/`의 커스텀 Tailwind 토큰과 스타일 규칙을 먼저 확인하고, 임의 값보다 정의된 토큰을 우선 사용한다.
- 코드 들여쓰기는 스페이스 2칸을 사용한다. 빈 줄에는 공백 문자를 남기지 않으며, 연속된 빈 줄은 하나만 허용한다.
- 함수 선언과 호출에서 인자가 두 개 이상이면 인자마다 줄바꿈하고 스페이스 2칸으로 들여쓴다. 인자 자체가 함수인 콜백도 별도 줄의 블록으로 작성한다. 한 줄에 너무 많은 구문을 넣지 말고, 가독성을 위해 줄바꿈과 들여쓰기를 적극적으로 사용한다.

```ts
const func = (
  onAction: () => void,
  items: unknown[],
) => {};

runTest(
  'test',
  (test) => {
    console.log('this is test');
  },
);
```

## 컴포넌트와 UI

- `pages/` 컴포넌트는 라우팅, 데이터 조합, 메타 설정, 렌더링 컴포넌트 호출만 맡는다. 페이지 파일 안에서 직접 UI를 구성하지 않는다.
- 화면 UI는 목적에 맞는 렌더링 컴포넌트에서 구성한다. 예를 들어 홈 화면 UI는 `components/home/Home.vue`에 두고 `pages/index.vue`에서는 해당 컴포넌트만 사용한다.
- 모든 렌더링 컴포넌트와 UI 컴포넌트는 CVA 구조를 사용한다.
- CVA 클래스 배열에서는 함께 적용되는 Tailwind 클래스를 원소별로 분리하지 않고 하나의 문자열 원소로 결합한다. 예: `'text-md', 'bg-white'` 대신 `'text-md bg-white'`로 작성한다.

```ts
const cssVariants = cva(
  [
    '',
  ],
  {
    variants: {},
    compoundVariants: [

    ],
    defaultVariants: {},
  },
);
```

- 템플릿의 클래스 병합에는 `utils/cn`을 적극적으로 사용한다.

```vue
<header
  :class="cn([
    cssVariants({}),
    props.class,
  ])"
/>
```

- 컴포넌트가 외부 클래스를 받을 때에는 `class` prop을 정의하고 `cn()`으로 병합한다.
- 시간과 날짜 처리는 가능한 Luxon을 사용한다.
- 모든 상호작용 함수는 `on<액션><대상>` 형식으로 이름을 짓는다. `handle*` 명명은 사용하지 않는다.

## 상태, 데이터, 서버

- 상태는 페이지 지역 상태, composable 공유 상태, Pinia store 순서로 확장한다.
- Pinia store는 도메인별로 나누고, UI 컴포넌트가 store 구현 세부 사항에 직접 의존하지 않게 한다.
- Pinia store 파일명은 반드시 `*.store.ts`로 통일한다.
- Pinia store를 사용할 때에는 store 인스턴스를 먼저 선언한다. 반응형 상태·getter는 `storeToRefs()`로 구조 분해하고, action은 store 인스턴스에서 직접 구조 분해한다. 상태 변경은 특별한 사유가 없는 한 직접 대입하지 않고 action을 통해 수행한다.

```ts
const appStore = useAppStore();

const {
  isSidebarOpen,
} = storeToRefs(appStore);

const {
  onSetSidebarOpen,
} = appStore;
```

- 서버 API는 `server/api/`에 두며, 입력 검증과 오류 응답 형식을 일관되게 유지한다.
- `any`를 사용하지 않는다. API, 폼, 데이터 경계에는 명시적 타입과 검증을 둔다.

## 검증과 Git

- 구현 완료라고 보고하기 전에 변경 범위에 맞는 검증 명령을 새로 실행한다.
- 기본 검증 후보는 `pnpm test`, `pnpm lint`, `pnpm exec vue-tsc --noEmit`, `pnpm build`다. 의존성이나 Nuxt 모듈 변경 뒤에는 `pnpm exec nuxi prepare`도 실행한다.
- 전체 검증이 기존의 무관한 문제로 실패하면, 변경 파일 대상 검증과 실패 원인을 분리하여 보고한다.
- 마스터의 요청 없이 커밋·푸시·PR 생성은 하지 않는다.
- 기본 브랜치는 `master`다.
- 모든 커밋 메시지는 `yyyy MMdd <type>: <한글 내용>` 형식의 날짜 접두와 커밋 타입을 사용한다.
- 변경사항이 많은 커밋은 본문에 불릿 목록으로 변경 내용을 상세히 기록한다.
